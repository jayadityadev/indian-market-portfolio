import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Indian Market Intelligence — Portfolio Analytics Platform",
  description:
    "ML-augmented backtesting and regime-aware strategy evaluation for Indian equities. Analyze NIFTY 50 and individual stocks with regime-conditional performance breakdowns.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link
          href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,600;9..144,700&family=Sora:wght@400;500;600;700&display=swap"
          rel="stylesheet"
        />
      </head>
      <body className="antialiased">{children}</body>
    </html>
  );
}
